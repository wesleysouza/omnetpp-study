#update and upgrade machine

sudo apt-get install build-essential gcc g++ bison flex perl python python3 qt5-default libqt5opengl5-dev tcl-dev tk-dev libxml2-dev zlib1g-dev default-jre doxygen graphviz

Problema 1: The Package 'libwebkitgtk-3.0-0' has no installation candidate. 

Entre em /etc/apt/sources.list

add: deb http://cz.archive.ubuntu.com/ubuntu bionic main universe

sudo apt update

sudo apt-get install libwebkitgtk-3.0-0

#OpenSceneGraph Library

sudo apt-get install openscenegraph-plugin-osgearth libosgearth-dev


#MPI Install

sudo apt-get install openmpi-bin libopenmpi-dev

#PCAP Install 

sudo apt-get install libpcap-dev

#Download

http://omnetpp.org

#Unpacking

tar xvfz omnetpp-5.6.2-src.tgz

#Conf Enviroment Variables

cd omnetpp-5.6.2
. setenv

gedit ~/.bashrc

-add in file:
export PATH=$HOME/omnetpp-5.6.2/bin:$PATH

You need to close and re-open the terminal for the changes to take effect.

# Configuring and Building 

In the top-level OMNeT++ directory, type:

./configure

If you are logged in a ssh session:

./configure WITH_TKENV=no WITH_QTENV=no

make

Verificando instalação:

cd samples/dyna
./dyna

# Starting IDE

-in terminal

# Install icons

make install-menu-item
make install-desktop-icon

#Extra packages

- Nemiver 

sudo apt-get install nemiver
omnetpp



